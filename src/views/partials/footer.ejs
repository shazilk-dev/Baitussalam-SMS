<% if (typeof pageCSS === 'undefined' || pageCSS !== 'dashboard') { %>
</main>
<hr>
<% } %>

<!-- Always load mobile menu for dashboard pages -->
<% if (typeof pageCSS !== 'undefined' && pageCSS === 'dashboard') { %>
<script src="/js/mobile-menu.js"></script>
<% } %>

<script src="/js/global.js"></script>

<% if (typeof pageJS !== 'undefined') { %>
<script src="/js/<%= pageJS %>.js"></script>
<% } %>

<%# 
  Footer removed for cleaner admin interface design.
  Admin systems typically don't need footers as they clutter the interface.
  The sidebar already provides navigation and branding.
%>
</body>
</html>
